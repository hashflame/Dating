namespace Blizka.App.Domain.Enums;

/// <summary>Кого показывать пользователю в ленте — предпочтение по полу, задаётся на шаге 2 онбординга (S-04).</summary>
public enum ShowGenderPreference
{
    Male,
    Female,
    All,
}
