/**
 * Версия документов, с которой согласился пользователь. Уходит на сервер и
 * фиксируется с временной меткой.
 *
 * Источник версии — здесь, а не на бэкенде: сами тексты тоже живут на фронте,
 * в локалях (`legal.*`), потому что это часть интерфейса и его перевода.
 *
 * **Поднимать вручную при каждой правке текстов `legal.*`** — иначе те, кто
 * согласился со старой редакцией, не будут переспрошены: `useConsentGiven`
 * сравнивает версию, а не только факт согласия.
 */
export const CONSENT_VERSION = '1.0'
