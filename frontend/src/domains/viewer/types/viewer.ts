/**
 * Текущий пользователь мини-аппа.
 *
 * ВАЖНО: поля ещё не сверены с контрактом бэкенда. Перед первым реальным
 * запросом найди соответствующий DTO в папке `backend/` и приведи тип к нему.
 */
export type Viewer = {
  id: string
  telegramId: number
  firstName: string
  /** Баланс «зорок» — внутренней валюты. */
  balance: number
  /** Пройден ли онбординг. От этого зависит стартовый экран. */
  isOnboarded: boolean
}
